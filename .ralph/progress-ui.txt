------------------------------------------------------------
[2026-06-11] TRACK INIT — Maersk Design UI refresh (branch: ui-maersk)
STATUS: READY
SCOPE: Frontend-only (apps/web). Tailwind replica of Maersk Design — Maersk Blue #42b0d5,
       deep #00a3e0, steel #b0c4d8, slate #6b7b8d, amber #f0b429; Inter + Fira Code.
       API, Prisma schema, contracts, and the anonymous share-link/name+role flow are UNCHANGED.
BACKLOG: .ralph/prd-ui-maersk.json — 4 phases (U1..U4), 13 stories. Active: U1.
RUNNER: .ralph/ralph-ui.ps1 -Iterations 5  (logs here)
ISOLATION: v1 record stays in .ralph/prd.json; v2 enterprise backlog lives on branch v2-enterprise.
           This branch merges to master -> Railway only on approval.
NOTES: Palette/stack derived from interrogating the live Maersk ALP platform, which itself uses
       plain Tailwind (no @maersk-global MDS component packages — those are private + not Railway-buildable).
------------------------------------------------------------
------------------------------------------------------------
[2026-06-12 10:06] STORY U1-S1 — Extend Tailwind theme with Maersk Design tokens
STATUS: DONE
CRITERIA:
  [PASS] tailwind.config.ts defines maersk.blue '#42b0d5', maersk.steel, maersk.slate, maersk.amber and sans=Inter / mono=Fira Code (grep confirmed all values present)
  [PASS] index.css base sets the Maersk surface/ink colors and a focus-visible ring (body bg-maersk-surface text-maersk-ink + *:focus-visible ring-maersk-blue)
  [PASS] npm run check (web) exits 0 — tsc --noEmit clean
  [PASS] npm run build (web) succeeds — vite build, 205 modules, dist emitted
  [PASS] npm run test exits 0 — api 12/12 passed, web no test files (exit 0)
NOTES: Added a Maersk Design palette under theme.extend.colors (maersk.blue #42b0d5, blue-deep #00a3e0, steel #b0c4d8, slate #6b7b8d, amber #f0b429, ink #141b25, surface #f0f4f8) plus a primary 50-900 scale aliased to Maersk Blue. Added fontFamily.sans=[Inter,...] / mono=[Fira Code,...], borderRadius.mds=0.5rem, and a subtle boxShadow.mds. index.css base layer now sets body bg-maersk-surface/text-maersk-ink/font-sans/antialiased and a global Maersk Blue focus-visible ring. No component markup changed (per story scope). Foundation-only; fonts are wired up in U1-S2.
------------------------------------------------------------
------------------------------------------------------------
[2026-06-12 10:09] STORY U1-S2 — Load Inter + Fira Code fonts (self-hosted, offline-safe)
STATUS: DONE
CRITERIA:
  [PASS] Inter and Fira Code bundled locally (no fonts.googleapis.com/gstatic) — grep of dist = 0 external font URLs; woff2 emitted under dist/assets/inter-*.woff2
  [PASS] Computed body font-family resolves to Inter — built index CSS contains the Inter @font-face; body uses font-sans (Inter) from the base layer
  [PASS] npm run check (web) exits 0 — tsc --noEmit clean
  [PASS] npm run build (web) succeeds — vite build, fonts emitted as hashed assets, built in ~1.7s
  [PASS] npm run test exits 0 — api 12/12 passed, web no test files (exit 0)
NOTES: Added @fontsource/inter@^5 and @fontsource/fira-code@^5 to apps/web dependencies and imported only the needed weights in main.tsx (Inter 400/500/600/700, Fira Code 400) ahead of './index.css'. Fonts are fully self-hosted: Vite fingerprints and bundles the woff2/woff files, so there is no runtime Google Fonts request — Maersk-network friendly and Railway-safe. No @maersk-global packages added (Tailwind-replica constraint respected). index.css base already maps body to font-sans=Inter from U1-S1, so the fonts apply without further markup changes.
------------------------------------------------------------
